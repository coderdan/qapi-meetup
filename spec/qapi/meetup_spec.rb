require 'spec_helper'

describe Qapi::Meetup do
  it 'has a version number' do
    expect(Qapi::Meetup::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
